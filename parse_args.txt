https://docs.python.org/3/library/argparse.html#formatter-class
+ le probleme:
  + programme 
     + pt entree du prog: boucle principale
       + espace de noms = module_de_parse_exterieur()
       + si option 1 en cli lancer ss prog 1 avec param x
       + si option 2 .....
         + le pb du lien entre cli et peuplement d'un dico avec valeur des options est adressé par argparse.	
     + sous programmes
       + sous prog 1:
        + stockage dans une base de donnée
       + sous prog 2:
        + interrogation de la base
        + calcul des heures
        + affichage du resultat
       
+ argparse
  + objet ArgumentParser
    + constructeur:
      prog=None, usage=None, description=None, epilog=None, 
      parents=[], formatter_class=argparse.HelpFormatter, 
      prefix_chars='-', fromfile_prefix_chars=None, argument_default=None, 
      conflict_handler='error', add_help=True, allow_abbrev=True
      + le tester avec un parents = [ liste d'args ] ou juste [ liste d args ]
    + methodes:
      + definir un modele en ajoutant des arguments et une manière de les gérer
        + l essentiel:
          + un argument obligatoire à valeur (bar)
          + un ou des arguments optionnels (-f ou --f ) à valeur ou pas 
        + add_argument 
      + renvoyer un espace de nom
       + parse_args : [c1] x [c2] x [c3] x [l] x  -> Obj
                      argument            espace de noms 
         + name or flags...[, action][, nargs][, const][, default][, type][, choices][, required][, help][, metavar][, dest])
+ utiliser un espace de noms:
  + "espace de noms" := objet qcq ss cl de obj possédant juste des attributs de valeur le nom de la valeur
     + premiere possiblité : sans transfo:
       + class c:
          pass
         c = c()
         parser = argparse.ArgumentParser()
         parser.add_argument(--foo)
         parser.parse_args(args=['--foo','prout'], namespace=c)
         c.foo ->  'prout'
       + 2eme possibilité avec transfo: 
         args = parser.parse_args(args=['--foo','prout'])
         plop = vars(args)  -> {'foo': 'prout'}
         

